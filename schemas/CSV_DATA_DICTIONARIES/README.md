# CSV data dictionaries

`TEMPLATE_FIELD_DICTIONARY.csv` is the authoritative Phase 14B field contract for every minimum and full template. `CONTROLLED_VOCABULARY.csv` defines locked values used by validators and records. Template headers must not be changed without updating these files, the record catalogue, route kits, workbook, crosswalks and semantic validators.
